##spring Properties profile Local
server:
  port: 8091
  servlet:
    application-display-name: Mutants
json:
  schemas:
    file: "/app/config/mutant_schema.json"
logging:
  level:
    com.skantuz.mutants: debug
    root: debug
spring:
  data:
    mongodb:
      database: mutants_db
      port: 27017
      host: mutants_mongodb